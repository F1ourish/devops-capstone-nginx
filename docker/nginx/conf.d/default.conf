server {
    listen 80;
    server_name _;

    location = /healthz {
        default_type text/plain;
        return 200 "ok\n";
    }

    location = / {
        default_type text/plain;
        return 200 "hello from nginx\n";
    }

    location / {
	    return 404;
    }
}

